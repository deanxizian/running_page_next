# Running Page UI 设计准则

本文档定义 Running Page 的界面设计规范。新增、修改或评审任何 UI
时，都应以此为准。Running Page 是一个 Strava 跑步数据仪表盘，
核心气质是：黑底、数据密集、路线可视、克制有力、方便长期回看。

## 1. 产品定位

Running Page 不是营销站，也不是个人主页的视觉展示页。它首先是一个
可用的跑步数据工具，第一屏必须直接呈现真实数据和主要操作，而不是
大标题、宣传语或介绍卡片。

界面应传达：

- 训练感：红色路线、目标进度、月度柱状图和日历热度要让人感到运动节奏。
- 档案感：多年跑步记录、赛事记录、热力图要像长期可靠的个人数据库。
- 精确感：距离、配速、心率、时长、日期都要便于比较。
- 克制感：黑色背景、低饱和面板、少量红色强调，避免花哨装饰。

避免：

- 首页 hero、营销式分屏、宣传文案堆叠。
- 纯装饰渐变、漂浮光斑、抽象插画、无数据意义的背景元素。
- 过大的标题出现在面板、表格、工具区域里。
- 在界面上解释功能、视觉样式或操作方式。
- 为了装饰而增加卡片、阴影或动画。

## 2. 信息架构

当前应用有三个主要页面：

- 首页 `/`：当前训练仪表盘。包括总里程、年度目标、月度目标、赛事入口、
  地图、日历、月度柱状图、活动日志。
- 热力图 `/heatmap`：多年跑步日历热力图，用来回看训练连续性和年度分布。
- 赛事记录 `/events`：按年份展示赛事或长距离活动，突出 PB 和路线。

导航标签固定为：

- `首页`
- `热力图`
- `赛事记录`

新增页面必须满足以下条件：

- 有独立且长期存在的用户任务。
- 不能只是首页某个面板的展开版本。
- 能保持三段式导航的紧凑性，或同时重构导航模式。

## 3. 整体视觉语言

### 3.1 黑底优先

应用只提供暗色主题，不保留亮色主题兼容分支。

主要背景色：

- 页面背景：`#000`
- 页脚背景：`#09090b`
- 面板/卡片背景：`#18181b`
- 地图兜底背景：`#202124`
- 悬停/选中背景：`#27272a`

主要文字色：

- 主文字：`#fff`
- 次级文字：`#d4d4d8`
- 辅助文字：`#a1a1aa`
- 元信息文字：`#71717a`
- 弱化/禁用文字：`#52525b`

强调色：

- 跑步红：`#e31937`
- 目标进度渐变：`linear-gradient(90deg, #e31937, #ff405c)`
- 地图省份填充：`#E31937`
- 单条路线颜色：`#E31937`
- 国家填充：`rgb(228,212,220)`

红色只能用于有明确意义的状态：

- 当前页面。
- 选中项。
- 跑步路线。
- 赛事/PB。
- 进度条。
- 可操作卡片的悬停或触摸揭示。

不要把红色用成大面积背景，也不要把红色作为普通装饰色到处铺开。

### 3.2 边框、圆角、阴影

默认面板/卡片：

- 边框：`1px solid #27272a`
- 圆角：桌面 `16px`
- 背景：`#18181b`
- 阴影：`0 0 20px -12px rgba(227, 25, 55, 0.28)`

悬停或预览状态：

- 边框变为红色透明：`rgba(227, 25, 55, 0.55)` 到
  `rgba(227, 25, 55, 0.75)`
- Dashboard 卡片上移 `2px`
- 赛事卡片上移 `4px`
- 使用低强度红色阴影，不做发光块

规则：

- 页面区块不要做成漂浮大卡片。
- 不要在卡片里再嵌套卡片。
- 需要分组时优先使用标题、分隔线、表格行、pill、内部留白。
- 圆角在移动端可降低到 `12px` 或 `14px`。

## 4. 字体和文字层级

字体：

- 主字体：`IBM Plex Sans`
- 等宽字体：`IBM Plex Mono`

使用规则：

- 标题、导航、活动名称、人类可读内容使用 Sans。
- 距离、时长、配速、日期、心率、年月、统计值使用 Mono。

字重：

- 大数字、标题、导航活跃态：`900`
- 控件、标签、表头：`800` 到 `900`
- 元信息：`500` 到 `700`

字距：

- 默认 `letter-spacing: 0`。
- 只有极小的元标签可以使用少量字距，例如赛事弹窗 eyebrow 的
  `0.08em`。

大小原则：

- 页面级标题可以大，但面板内部标题应紧凑。
- 面板标题通常约 `20px`。
- 指标值通常约 `28px`。
- 表格、chip、辅助信息以 `11px` 到 `14px` 为主。
- 不要为了适配移动端无限缩小文字；优先换行、重排或横向滚动。

## 5. 页面布局

### 5.1 全局外壳

`.page`：

- 宽度 `100%`
- 最小高度 `100vh`
- 黑色背景
- 禁止横向溢出

`.main`：

- 最大宽度 `1280px`
- 水平居中
- 顶部 padding：`clamp(86px, 8vw, 96px)`，用于避开固定导航
- 左右 padding：`clamp(16px, 3vw, 24px)`
- 底部 padding：约 `38px`

任何新页面都应从实际功能内容开始，不要插入营销式首屏。

### 5.2 固定导航

导航固定在顶部：

- 位置：`fixed`
- 层级：`z-index: 50`
- 背景：`rgba(0, 0, 0, 0.32)`
- 背景滤镜：`blur(64px) saturate(1.9) contrast(1.12)`
- 内部最大宽度：`1280px`
- 高度：`clamp(58px, 6vw, 64px)`

品牌：

- 文案：`Running Page`
- `Running` 为白色
- `Page` 为红色
- 字重 `900`
- 不换行

导航链接：

- 三等分 grid。
- 外层是胶囊形容器。
- 当前页面文字为红色。
- 当前页面有红色半透明 indicator。
- indicator 通过 transform 移动，不改变布局。

规则：

- 导航只承载顶层页面。
- 不加说明文字。
- 不加图标，除非整体导航体系重做。

### 5.3 首页布局

桌面布局：

- 主 grid：左列 `minmax(0, 3fr)`，右列 `minmax(360px, 1.95fr)`
- 间距：`30px`
- 左列：指标卡片、活动日志
- 右列：赛事入口、地图、日历、月度柱状图

`max-width: 1120px`：

- 变为两列流式布局。
- 左右列容器使用 `display: contents`。
- 指标、活动日志、赛事入口、地图可跨整行。

`max-width: 760px`：

- 单列布局。
- 间距降低到 `24px`。
- 面板圆角和 padding 缩小。

`max-width: 520px`：

- 指标卡片变为两列。
- Total Distance 横跨两列。
- 卡片内部布局重排，避免数字挤压。

### 5.4 大、中、小尺寸模块摆放矩阵

所有页面都必须按照下列尺寸规则摆放模块。除非明确重构布局系统，
不要在单个组件里临时改变这些顺序。

尺寸定义：

- 大屏：`1121px` 及以上。
- 中屏：`761px` 到 `1120px`。
- 小屏：`521px` 到 `760px`。
- 超小屏：`520px` 及以下。

#### 首页模块顺序和对齐

大屏：

- 页面内容最大宽度 `1280px`，整体水平居中。
- 主 grid 为两列：左列宽、右列窄。
- 左列从上到下：
  - Metrics Grid，三张指标卡同一行，顶部对齐。
  - Activity Log，位于指标卡下方，宽度占满左列。
- 右列从上到下：
  - Event Summary Card。
  - Map Panel。
  - Calendar Panel。
  - Monthly Chart Panel。
- 左右列顶部对齐。
- 左右列内部间距保持一致，不因为单个面板内容变化而重新排序。
- Activity Log 表格在左列内居中，宽度上限 `860px`。

中屏：

- 主 grid 变为两列流式布局。
- 模块顺序固定为：
  - Metrics Grid：跨两列，占整行。
  - Event Summary Card：跨两列，占整行。
  - Map Panel：跨两列，占整行。
  - Calendar Panel：占一列，左侧优先。
  - Monthly Chart Panel：占一列，右侧优先，并拉伸占满可用宽度。
  - Activity Log：跨两列，占整行，放在最后。
- Calendar 和 Monthly Chart 顶部对齐；如果高度不同，不强行等高，
  但 chart 要保持自身内部图表高度稳定。
- Activity Log 仍保持表格居中；不足宽度时只允许表格内部横向滚动。

小屏：

- 主 grid 变为单列。
- 模块顺序固定为：
  - Metrics Grid。
  - Event Summary Card。
  - Map Panel。
  - Calendar Panel。
  - Monthly Chart Panel。
  - Activity Log。
- 所有模块宽度占满内容列。
- 面板圆角降为 `12px` 到 `14px`。
- Calendar、Chart、Map 都必须保留稳定高度，不能随 hover/preview 改变。
- Activity Log 表格不压缩到不可读；保持 `640px` 左右最小宽度并横向滚动。

超小屏：

- Metrics Grid 变为两列。
- Total Distance 横跨两列并作为第一张卡。
- Yearly Goal 和 Monthly Goal 各占一列。
- Event Summary Card 使用两列内部布局；latest finish 横跨两列并放到下方。
- 所有面板的左右 padding 收紧，但不能低于可读间距。
- 长标题、活动名、赛事名优先 ellipsis 或换行，不允许遮挡按钮。

#### 热力图页面模块顺序和对齐

大屏：

- 页面内容最大宽度 `1280px`，水平居中。
- 顶部总览统计靠右对齐。
- 年份列表纵向排列。
- 每个年份块中，年份数字在左，统计 chips 在右，顶部对齐。
- 热力图 grid 占满年份块宽度，左对齐开始。

中屏：

- 顶部总览统计仍靠右。
- 年份块保持单列纵向排列。
- 年份 header 仍为左年号、右 stats。
- 如果 stats 宽度不足，允许 chips 收紧间距，但不换成表格或隐藏。

小屏：

- 顶部总览统计改为纵向区域，仍靠右/末端对齐。
- 年份块继续单列排列。
- 年份 header 保持两列：左年号、右 stats；stats 可以纵向堆叠。
- 热力图 gap 缩小，格子仍保持接近正方形。

超小屏：

- 年份 header 可变为单列。
- stats 占满年份块宽度并靠右或末端对齐。
- 不允许把年份块改成横向滑动卡片。

#### 赛事页面模块顺序和对齐

大屏：

- 页面主区域可以取消默认左右 padding，但内容容器仍限制在 `1280px`。
- 年份组纵向排列，组间距约 `80px`。
- 年份 header 横向排列：
  - 左侧大号低透明年份。
  - 中间红色事件说明。
  - 右侧次级元信息。
- 赛事卡片 grid 为 3 列，所有卡片顶部对齐。

中屏：

- 年份组顺序不变。
- 赛事卡片 grid 改为 2 列。
- 年份 header 仍保持横向排列；右侧元信息可以收紧，但不隐藏。

小屏：

- 赛事卡片 grid 改为 1 列。
- 年份 header 改为纵向堆叠。
- 年份数字降到约 `52px`。
- 卡片宽度占满内容列。

超小屏：

- 卡片仍为 1 列。
- PB badge 保持右上角；如果标题过长，标题换行或截断，不能压住 badge。
- Modal 仍居中显示，外层 padding 保持 `24px` 左右；必要时内部滚动，
  不让 modal 超出视口。

#### 固定导航在各尺寸下的对齐

大屏：

- `.navInner` 最大宽度 `1280px`，居中。
- Brand 左对齐，导航 pill 右对齐。

中屏：

- Brand 和导航 pill 保持同一行。
- 导航 pill 使用 `clamp` 宽度，不挤压 brand。

小屏：

- Brand 仍在左，导航 pill 仍在右。
- 导航字号保持 `12px` 左右。
- 不换成汉堡菜单。

超小屏：

- 导航 pill 最大使用 `min(55vw, 232px)`。
- `max-width: 340px` 时导航字号可降到 `11px`。
- Brand 不换行；如果空间不足，优先压缩 nav link padding。

## 6. 通用组件规范

### 6.1 Panel

Panel 是主要的信息容器。

默认样式：

- 背景：`#18181b`
- 边框：`#27272a`
- 圆角：`16px`
- 溢出隐藏
- 进入动画：`moduleFadeIn 0.68s cubic-bezier(0.22, 1, 0.36, 1)`

Panel header：

- padding：`26px 26px 18px`
- 标题：白色、`20px`、`900`
- 右侧元信息：`#71717a`、`14px`

移动端：

- header 可纵向堆叠。
- padding 降至 `22px 18px 16px`。

适合使用 Panel 的内容：

- 活动日志。
- 日历。
- 月度柱状图。
- 热力图年份块。
- 弹窗主体。

不适合使用 Panel 的内容：

- 整个页面级 section。
- 纯装饰分组。
- 已经在卡片里的二级卡片。

### 6.2 Metric Card

指标卡是首页的第一层信息。

当前三张卡：

- Total Distance：总里程、跑步次数、总时长、最近一次跑步，点击进入热力图。
- Yearly Goal：年度里程、年度次数、年度时长、3000 km 目标进度、
  与去年同期对比。
- Monthly Goal：月度里程、月度次数、月度时长、300 km 目标进度、
  与上月同期对比。

桌面样式：

- 三等分 grid。
- 最小高度 `150px`。
- padding `20px`。
- 内部 grid 预留 label、value、progress、details、trend/footer。

文字层级：

- Label：Mono、uppercase、`12px`、`700`、灰色。
- Value：Mono、白色、`28px`、`900`。
- Unit：灰色、小字号。
- Details：灰色，配红色小图标。
- Trend/Footer：灰色、粗体、小字号。

交互：

- 可点击卡片必须是 `button`。
- hover 或 touch reveal 时显示黑色半透明 overlay。
- overlay 文案必须短，不写长说明。
- 触摸 reveal 要能重置其他 reveal 状态，避免多个 overlay 同时打开。

规则：

- 一张指标卡不要超过两个 detail。
- progress/trend 区域即使为空也要预留空间，避免跳动。
- 数字始终使用等宽字体。

### 6.3 Button 和 Icon Button

方向按钮用于：

- 分页。
- 日历月份切换。
- 月度图表年份切换。

默认样式：

- 尺寸：`36px x 32px`
- 圆角：`8px`
- 背景：`#27272a`
- 图标/文字：`#d4d4d8`
- hover：红底白色
- active：`scale(0.95)`
- disabled：opacity `0.35`

规则：

- 方向操作使用图标，不使用“上一页”“下一页”等长按钮。
- 图标按钮必须有 `aria-label`。
- 禁用状态使用原生 `disabled`。
- 状态变化不能改变按钮尺寸。

### 6.4 Pill

Pill 用于紧凑选项或状态：

- 年份筛选。
- PB badge。
- 弹窗统计值。
- 热力图统计 chip。

默认：

- 圆角 `999px`
- 背景 `#27272a` 或透明白色弱背景
- 激活态红底白字
- 字重较高

规则：

- Pill 是状态或筛选，不是段落容器。
- 不用 pill 承载长文本。

## 7. 首页具体模块

### 7.1 活动日志

活动日志是高密度表格。

结构：

- Header：`Activity Log` 和当前显示范围。
- 年份筛选：年份加 `All`。
- 表格列：Date、Name、Distance、Duration、Pace、HR。
- Pagination：左按钮、页码、右按钮。

表格布局：

- 使用固定 grid 列，不依赖浏览器自动表格布局。
- 桌面宽度：`min(100%, 860px)`。
- 最小宽度：`680px`。
- 移动端允许横向滚动。

表格视觉：

- 表头 uppercase、灰色、粗体。
- 数据值使用等宽字体。
- 活动名称使用 Sans。
- 距离和时长更强调。
- 行分隔线使用低透明度 inset 线。

交互：

- 点击行选中跑步记录。
- 选中行同步地图、日历和当前页面状态。
- 选中行背景：`rgba(39, 39, 42, 0.58)`。
- keyboard focus 必须可见。

规则：

- 不随意新增列；新增列必须有明确比较价值。
- 大多数 cell 不换行。
- 日期 cell 可以拆成日期和时间两行。

### 7.2 赛事入口卡

首页赛事入口是 `/events` 的摘要。

结构：

- 左侧大数字：当前年份赛事数。
- 中间标题：赛事记录/摘要信息。
- 右侧 latest finish。

样式：

- 大数字红色、Mono、`58px`、`900`。
- 标题白色、粗体、uppercase。
- latest finish 桌面有左分隔线。
- 移动端 `max-width: 520px` 时 latest finish 横跨两列并改为上分隔线。

规则：

- 这里只做摘要，不展开赛事列表。
- 详细浏览必须进入 Events 页面。

### 7.3 地图面板

地图是路线摘要，不是完整交互地图。

行为：

- 地图只使用 MapCN：MapLibre GL + CARTO dark-matter。
- 不读取 Mapbox token，也不保留其他地图 provider 分支。
- `interactive={false}`。
- 相机由选中跑步或当前活动集合驱动。
- 首次加载保留由远到近的相机动画。
- 地图加载时保留高度。
- 地图错误显示 overlay。

视觉：

- 背景 `#202124`。
- 路线红色。
- 户外路线更实，室内路线更淡。
- 非重点路线可 dim。
- 省份红色低透明填充，国家淡色填充。

规则：

- 不添加缩放控件、marker 堆叠或弹窗，除非重做地图交互模型。
- 路线是数据，不是装饰。
- 地图高度要稳定。

### 7.4 日历面板

日历用于按月查看跑步分布。

Header：

- 月份格式：`YYYY/MM`。
- 同行显示月总里程。
- 右侧有上月/下月图标按钮。

Grid：

- 一周从 Monday 开始。
- 7 列。
- 固定补齐 42 个 cell，保持高度稳定。
- 有跑步的日期显示 route spark。
- 无 route spark 时显示红点。

交互：

- hover 显示日期元信息 overlay。
- touch 使用 pointer preview 模拟 hover。
- 点击日期里的跑步记录会同步选中活动。
- 切月使用左右滑入动画。

规则：

- cell 尺寸固定，hover overlay 不能撑开布局。
- 不可选月份按钮要显示但 disabled。
- 未来月份选择上限应符合当前产品逻辑。

### 7.5 月度柱状图

月度图展示某一年 12 个月的跑量。

结构：

- Header：标题和年份切换按钮。
- 12 等分柱。
- value label 默认隐藏，active/hover/preview 时显示。

视觉：

- 默认柱：`#52525b`
- active 柱：红色
- active 阴影：低强度红光
- disabled 月份：opacity `0.35`
- 月份 label 居中，小字号

规则：

- 不用多色分类；高度表示距离，红色表示当前关注。
- 保持 12 列节奏。
- 移动端高度提高到 `clamp(200px, 50vw, 240px)`。

## 8. 热力图页面

热力图页面用于长期回看。

顶部统计：

- 右对齐。
- 三个统计：runs、km、hours。
- 每个统计是紧凑深色块。
- 数字白色 Mono，单位红色 uppercase。

年份块：

- 每年一个 Panel。
- 左侧年份，右侧统计 chips。
- 每日格子按 7 行、按周向右流动。
- 宽高比由周数决定。

颜色等级：

- 年外日期：透明。
- 无跑步：`#27272a`。
- Level 1：`#075985`。
- Level 2：`#0284c7`。
- Level 3：`#ca8a04`。
- Level 4：`#e31937`。

交互：

- hover/preview 用白色 inset ring。
- touch preview 与 hover 行为一致。
- 可用浏览器 title 显示日期和距离。

响应式：

- `max-width: 700px`：缩小 gap 和 chip。
- `max-width: 360px`：年份 header 堆叠。

规则：

- 格子保持接近正方形。
- 不做横向年份轮播。
- 图例如果增加，必须紧凑，不能压过数据本体。

## 9. 赛事页面

赛事页面是按年份分组的赛事档案。

页面布局：

- 主内容可突破默认左右 padding。
- 内容最大宽度仍为 `1280px`。
- 年份组之间使用大间距，当前约 `80px`。

年份标题：

- 年份数字超大、低透明度。
- 年份下方有细分隔线。
- 赛事数量/说明使用红色。
- 次级信息靠右，灰色。

赛事卡片：

- 桌面 3 列。
- `max-width: 1120px` 时 2 列。
- `max-width: 760px` 时 1 列。
- 最小高度 `162px`。
- 圆角 `18px`。
- 背景 `#18181b`。
- 边框为弱白色透明。
- 背景路线为低透明红色 SVG。

卡片内容：

- 日期：红色 Mono，小字号，粗体。
- 标题：白色，约 `20px`，`900`。
- 时长：Mono，灰色。

PB 状态：

- 卡片带红色斜向渐变。
- 右上角红色 PB badge。
- Badge 内使用 medal icon。

交互：

- hover/touch preview：上移、红边、红阴影、路线背景更明显。
- 点击打开赛事 modal。

赛事 modal：

- backdrop 固定全屏。
- 黑色半透明背景。
- 背景 blur 和 saturate。
- modal 宽度 `min(100%, 668px)`。
- 圆角 `24px`。
- padding `24px`。
- 统计项用 pill。
- 内嵌地图高度 `260px`。
- Escape 可关闭。
- 关闭动画结束后再卸载。

规则：

- 赛事卡片必须是 button。
- 路线背景必须来自真实路线数据。
- 不在赛事页加密集表格；详情放 modal。
- Strava `workout_type=1`（Race）的跑步直接计入赛事。
- 没有 Race 标记时，仅在标题与距离同时匹配时兜底识别：半马
  `20–23 km`，全马 `40–45 km`。
- PB 只对赛事中的半马和全马计算；其他距离的 Race 只展示赛事记录。

## 10. 动效规范

主要动效：

- 模块进入：`0.68s cubic-bezier(0.22, 1, 0.36, 1)`。
- 活动行进入：`0.36s cubic-bezier(0.22, 1, 0.36, 1)`。
- 日历月份切换：`0.54s cubic-bezier(0.22, 1, 0.36, 1)`。
- 导航 indicator：每步 `340ms`。
- Modal backdrop：进入约 `0.38s`，退出约 `0.36s`。
- Modal 主体：进入约 `0.42s`，退出约 `0.34s`。

原则：

- 动效用于提示页面、月份、筛选或弹窗状态变化。
- 不做持续循环动画。
- 不做弹跳、夸张缩放、漂浮光效。
- hover 位移小而明确。
- 不通过动画改变布局尺寸导致内容跳动。

Reduced motion：

- 必须尊重 `prefers-reduced-motion: reduce`。
- 模块进入、行进入、日历滑动、热力图 header、赛事卡片、modal 动画都要可关闭。

## 11. 响应式规则

主要断点：

- `1120px`：首页布局重排；赛事卡从 3 列变 2 列。
- `760px`：单列页面；赛事卡 1 列；面板 padding 和圆角收紧。
- `520px`：指标卡变 2 列；Total Distance 跨两列；赛事入口堆叠。
- `429px` 及以下：非总计指标卡 detail 纵向排列。
- `360px`：热力图年份 header 堆叠。
- `340px`：导航字号进一步缩小。

规则：

- 除活动表格外，尽量避免横向溢出。
- 表格允许横向滚动，因为数据密度高。
- 文本不能重叠或被按钮遮挡。
- 控件点击区域保持稳定。
- 优先重排和换行，不要无限缩小字体。

## 12. Hover、Touch 和 Pointer

桌面 hover 必须写在：

```css
@media (hover: hover) and (pointer: fine) {
  /* Hover-only styles */
}
```

原因：

- 避免移动端产生粘滞 hover。
- 让 touch preview 由 React pointer 状态控制。

Touch 规则：

- 日历、月度柱、热力图、赛事卡都要有 touch preview。
- 不依赖 CSS hover 展示移动端关键信息。
- 触摸 reveal 同一时间只应有一个主要 overlay。

Cursor：

- 可点击元素使用 pointer。
- disabled 控件使用 default cursor。
- 地图当前不可交互，不显示交互型控件。

## 13. 可访问性

必须做到：

- 使用语义元素：`nav`、`main`、`section`、`aside`、`table`、`button`。
- 可点击卡片使用 button 或 link。
- 图标按钮必须有 `aria-label`。
- disabled 状态使用原生 `disabled`。
- 键盘 focus 可见。
- Modal 支持 Escape 关闭。
- Modal 打开时锁定 body scroll。
- 装饰性路线背景不应干扰读屏。

对比度：

- 主信息用白色或接近白色。
- 红色用于关键信息时必须有足够对比。
- 灰色只用于辅助信息，不承载唯一关键信息。

## 14. 数据格式

距离：

- UI 单位为 `km`。
- 内部数据以米为主。
- 目标卡可显示一位小数。
- 年度/热力图总览可显示整数。

时长：

- 单次活动：`H:MM:SS`。
- 累计时长：紧凑显示，如 `3h 20m`。
- 配速：每公里分钟秒。

日期：

- 活动日志可拆分日期和时间。
- 日历标题：`YYYY/MM`。
- 赛事卡：`YYYY-MM-DD`。

缺失值：

- 如果 0 是真实数据，可以显示 0。
- 如果数据不可用，优先显示 `-`，不要伪装成 0。

## 15. 文案语言

当前界面是中英混合：

- 导航为中文。
- 指标标签、表头、部分统计标签为英文。
- 部分操作 overlay 为中文。

规则：

- 文案短、直接、功能性强。
- 不写教程式说明。
- 不在 UI 上解释视觉设计或实现细节。
- 新增文案应匹配所在组件的语言环境。
- 英文普通文案用 sentence case；已有紧凑 label 可继续 uppercase。

## 16. 地图和路线

路线是核心视觉资产，必须来自真实活动数据。

路线使用：

- 日历：小型 route spark。
- 赛事卡：低透明度路线背景。
- 赛事 modal：更清晰的单条路线地图。
- 首页地图：当前筛选或选中活动路线集合。

地图图层：

- 户外路线优先级高。
- 室内路线低透明度显示。
- 非重点路线 dim。
- 省份/国家填充只提供地理上下文。

禁止：

- 使用亮色底图抢路线注意力。
- 加大量 marker 干扰路线形态。
- 在没有重新设计交互模型前打开主地图交互。

## 17. Loading、Empty、Error

已有模式：

- Lazy map fallback 保留最终高度。
- Map style shield 柔化底图加载。
- Map error overlay 使用红色通知。
- activities 数据 schema 错误直接抛出。

规则：

- Loading 状态必须保留最终布局尺寸。
- Empty 状态要安静、紧凑。
- Error 要可理解，但不要写成长段说明。
- 不用大插画替代数据面板。

## 18. 前端实现规范

目录职责：

- `src/app`：路由和整体布局。
- `src/entities/activity`：活动领域模型、统计、日期、路线。
- `src/features`：页面级功能。
- `src/shared/ui`：通用 UI 原语。
- `src/shared/map`：地图组件和图层。
- `src/shared/theme`、`src/shared/config`：主题和配置。

CSS：

- 页面和组件样式优先用 CSS Modules。
- 全局 CSS 只放主题变量和 base 样式；MapLibre 样式随地图组件懒加载。
- 响应式规则尽量靠近对应组件样式。
- 新增颜色和尺寸前先查现有 token/类。

组件：

- View 组件尽量展示化。
- 复杂状态放到 model hook 或 selector。
- 复用现有 `MetricCard`、`RouteSpark`、`ChevronIcon`、`PageShell`。
- 新增 UI pattern 时同步更新本文档。

## 19. UI 变更评审清单

提交任何 UI 变更前检查：

- 是否保持黑底数据仪表盘气质？
- 红色是否只用于有意义的强调？
- 是否避免了营销页/hero 风格？
- 是否没有卡片套卡片？
- 数字格式是否和现有页面一致？
- 文本在桌面、平板、窄屏手机上是否不重叠？
- hover 是否只写在 pointer/hover media query 内？
- touch 是否有等价 preview 或 reveal？
- 图标按钮是否有 `aria-label`？
- disabled 是否是真 disabled？
- reduced motion 下是否仍可用？
- 地图、表格、图表是否保留稳定尺寸？
- 是否复用了现有 CSS Module 和组件模式？
- 如果影响布局，是否在浏览器里实际看过？

## 20. 何时可以扩展设计系统

只有满足以下条件时才新增设计模式：

- 现有组件无法清晰表达新数据。
- 强行复用旧模式会造成理解错误。
- 新模式能在多个地方复用，或代表新的稳定页面能力。
- 新模式已考虑桌面、平板、移动端。
- 同一个提交中更新 `DESIGN.md`。
